use crate::core::process::Process;
use std::collections::VecDeque;
use std::vec::Vec;

const context: f32 = 0.2;

pub fn srtf(queue: &mut Vec<Process>) -> () {
    let mut timer: f32 = 0.0;
    let mut finished: f32 = 0.0;
}
